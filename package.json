{
  "name": "pythonanywhere-keep-site-running",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright scripts for extending and reading PythonAnywhere web app expiry dates.",
  "scripts": {
    "extend": "node scripts/extend-run-until.js",
    "get-date": "node scripts/get-best-before-date.js",
    "install:browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "playwright": "^1.59.1"
  },
  "devDependencies": {}
}
